<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home_index') }}" title="Accueil">Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('ingredient_index') }}" title="Les ingrédients">Les ingrédients
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('recipe_index') }}" title="Les recettes">Les recettes
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
            </ul>
            <div class="d-flex">
                {% if app.user %}
{#                    <a class="btn btn-primary" href="{{ path('user_edit', {'id': app.user.id}) }}" title="Mon profil">Mon profil</a>#}
                <span class="dropdown">
                        <a class="btn btn-primary dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Mon profil</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('user_edit', {'id': app.user.id}) }}" title="Éditer mon profil">Éditer mon profil</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('user_edit_password', {'id': app.user.id}) }}" title="Éditer mon mot de passe">Éditer mon mot de passe</a>
                        </div>
                </span>
                    <a class="btn btn-primary" href="{{ path('security_logout') }}" title="Se déconnecter">Déconnexion</a>
                {% else %}
                    <a class="btn btn-primary" href="{{ path('security_login') }}" title="Se connecter">Connexion</a>
                    <a class="btn btn-primary" href="{{ path('security_signup') }}" title="S'inscrire">Inscription</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>